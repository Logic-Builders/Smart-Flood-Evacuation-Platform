# Copy to .env and customize before running the app.
# cp .env.example .env
#
# Must be your dev machine's LAN IP (not localhost) so a physical device or
# emulator on the same network can reach the backend.
EXPO_PUBLIC_API_URL=http://10.10.11.136:8080
